/**
 * @overview Store holding notifications for "Toast" displays
 * @author Ira Burton <ira.burton+gh@gmail.com>*
 */

import { writable } from 'svelte/store'

const store = writable({})

export default store
